# =============================================================================
# PENDLE MCP SERVER CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# Pendle Finance API Configuration
# -----------------------------------------------------------------------------
# The Pendle API v2 endpoint for fetching market data
# Chain ID: 1 = Ethereum Mainnet, 42161 = Arbitrum, 10 = Optimism
PENDLE_API_URL=https://api-v2.pendle.finance/core/v1/1/markets
PENDLE_CHAIN_ID=1

# API request timeout in seconds
API_TIMEOUT=10

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Ethereum Blockchain Configuration (OPTIONAL)
# -----------------------------------------------------------------------------
# These are only needed if you want to enable real blockchain transactions.
# For testing and simulation, you can leave these empty.

# Ethereum RPC URL (e.g., Infura, Alchemy, or local node)
# Example: https://mainnet.infura.io/v3/YOUR_PROJECT_ID
# Example: https://sepolia.infura.io/v3/YOUR_PROJECT_ID (testnet)
RPC_URL=

# Private key for wallet (NEVER use your main wallet with real funds!)
# Leave empty to run in simulation-only mode
# Example: 0x1234567890abcdef... (64 hex characters after 0x)
PRIVATE_KEY=

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit your .env file with real private keys to version control
# 2. NEVER use your main wallet private key - use a test wallet only
# 3. For production, use environment variables or secure secret management
# 4. The .gitignore file should include .env to prevent accidental commits
# =============================================================================
